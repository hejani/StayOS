"""Escalation Service component (``pulse-escalation-service``).

Owns two concerns: the escalation-trigger hierarchy (mandatory GM review) and
the time-based escalation routing chain (GM then AGM then MOD).

Implementation is added in later tasks; this module currently only marks the
sub-package.
"""

__all__: list[str] = []
