"""Triage Agent component (``pulse-triage-agent``).

For a CRITICAL or WARNING alert, generates a triage brief (summary, confidence,
ranked options) by calling Amazon Bedrock with a per-alert-type prompt.

Implementation is added in later tasks; this module currently only marks the
sub-package.
"""

__all__: list[str] = []
