"""Backend services package for StayOS.

This package contains long-running services (ECS Fargate containers)
as opposed to Lambda functions. Each sub-package defines one deployable
service with its own Dockerfile and requirements.
"""
