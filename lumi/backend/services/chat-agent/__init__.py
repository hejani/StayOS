"""StayOS Chat Agent package.

Marks `backend/services/chat-agent` as a Python package so its modules
(`server`, `identity_resolver`, `system_prompt`) can be imported relative to
one another when the AgentCore Runtime container starts `server.py`.
"""
