{
  "name": "@stayos/auth",
  "version": "1.1.0",
  "private": true,
  "description": "Shared StayOS client-side auth module (single sign-on across the shell, LUMI, and PULSE).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "fast-check": "^3.19.0",
    "jsdom": "^30.0.1",
    "typescript": "5.8",
    "vitest": "^4.1.10"
  }
}
