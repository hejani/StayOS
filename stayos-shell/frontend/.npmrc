# @testing-library/react 15 declares a React 18 peer range but works with the
# React 19 version this project pins (matching LUMI/PULSE's installed tree).
# Enable legacy peer resolution so `npm install` is reproducible without --force.
legacy-peer-deps=true
